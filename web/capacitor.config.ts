import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.seraj.munir',
  appName: 'Al-Siraj Al-Munir',
  webDir: 'dist',
  server: {
    androidScheme: 'https',
  },
};

export default config;